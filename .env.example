PORT=3000

# Local connection string used by the app, credentials define in docker-compose.yml, use to run locally.
DB_URL="host=localhost user=gin_user password=gin_password dbname=gin_proyect port=5432 sslmode=disable"

# Swagger UI base URL if deployed (host + scheme).
# PUBLIC_BASE_URL=https://your-app.com